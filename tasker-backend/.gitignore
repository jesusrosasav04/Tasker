# Dependencias
node_modules/

# Variables de entorno (NUNCA subir)
.env
.env.local
.env.production

# Llaves JWT (NUNCA subir la privada al repo)
keys/*.pem
keys/*.key

# Logs
logs/
*.log
npm-debug.log*

# Sistema operativo
.DS_Store
Thumbs.db